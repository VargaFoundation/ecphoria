[package]
name = "ecphoria-cluster"
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Distributed clustering for the Ecphoria context lake"

[features]
default = ["agentic"]
# Apply agent-run log entries. The `AppRequest` variants exist either way — they are the wire
# format, and removing them would shift MessagePack's positional encoding — but a node built
# without this has no ledger to apply them to and skips them with a warning.
agentic = ["ecphoria-core/agentic"]

[dependencies]
ecphoria-core.workspace = true

tokio.workspace = true
futures.workspace = true
async-trait.workspace = true

openraft.workspace = true

# gRPC transport for inter-node Raft RPCs (HTTP/2 + binary).
tonic.workspace = true
tonic-prost.workspace = true
prost.workspace = true
# TLS crypto provider for the Raft transport (installed once before tonic TLS use).
rustls = { version = "0.23", default-features = false, features = ["ring"] }

serde.workspace = true
serde_json.workspace = true
rmp-serde.workspace = true

thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
metrics.workspace = true

uuid.workspace = true
chrono.workspace = true
bytes.workspace = true
parking_lot.workspace = true
rusqlite.workspace = true

[build-dependencies]
tonic-prost-build.workspace = true
protobuf-src.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
toml.workspace = true
tempfile.workspace = true
rcgen = "0.13"
