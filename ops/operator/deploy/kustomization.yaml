# Deploy the operator: kubectl apply -k ops/operator/deploy
# (installs the CRD, namespace, RBAC and the controller Deployment).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - crd.yaml
  - operator.yaml   # Namespace + Deployment
  - rbac.yaml
